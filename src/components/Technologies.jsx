

const Technologies = ({theme}) => {
  return (
    <div>
      <h1 className={`${theme==='dark' ? 'text-neutral-200' : 'text-gray-400'} mt-64 pb-24 text-2xl lg:text-4xl text-center items-center justify-center `}>
        Technologies
      </h1>
      <div> 
        
      </div>
        <div>

        </div>
    </div>
  )
}

export default Technologies;
